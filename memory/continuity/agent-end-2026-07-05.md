# Agent-end continuity drops (2026-07-05)

## 2026-07-05T00:00:46.938Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T00:10:46.447Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T00:20:45.249Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T00:30:44.972Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T00:40:45.306Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T00:50:44.618Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T01:00:48.118Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T01:10:46.591Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T01:20:42.542Z · fact · session 71920bf8-c17f-49e8-9572-188d1197f20c

Build finished with 17 errors — all scintilla macro poisoning. I removed all `#undef` directives earlier which broke the fix. Need to restore them (minus `SCK_*` which shortcut.h needs) and force PCH rebuild:

## 2026-07-05T02:54:19.020Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

The ScintillaComponent/Scintilla.h macro conflict errors are **fixed**. The build progressed significantly — ScintillaComponent.h now compiles cleanly. Here's a summary of what was fixed:

**Root causes resolved:**
1. **`SC_ELEMENT_HOT_SPOT_ACTIVE` shadow** — renamed `setElementColour(int element, ...)` parameter to `sciElement` to avoid conflicting with Scintilla.h's `#define SC_ELEMENT_HOT_SPOT_ACTIVE 322`
2. **Missing constants** — added `SCI_SETELEMENTCOLOUR`, `SCI_SETPRINTCOLOURMODE`, `SCI_FORMATRANGEFULL` to `npp_sci` namespace
3. **`#pragma once` deadlock** — removed `#pragma once` from `NppSciCompat.h` so `#undef` directives run on re-inclusion (moc files)
4. **`shortcut.h` fallback** — added self-contained `#undef SCK_*` / `#undef SC_UPDATE_*` with `using npp_sci::` declarations
5. **`Parameters.h` pollution** — added `#undef SC_PRINT_*`, `SC_MOD_*`, `SC_PERFORMED_*`, `SCI_STYLEGET*` before uses of `npp_sci::` constants
6. **`NppConstants.h` conflict** — added `#undef INDICATOR_*` before `inline constexpr int INDICATOR_*` declarations

**Remaining errors** (not in original scope, likely snapshot divergence per HEARTBEAT):
- `Notepad_plus.cpp`, `NppBigSwitch.cpp`, `Parameters.cpp` — hundreds of issues from a stale flat snapshot (4376-line `Parameters.cpp` vs git's 3479 lines, per HEARTBEAT audit)

## 2026-07-05T03:13:39.638Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

Found it! `PluginInterface.h` includes `Scintilla.h`. I need to add `#undef` for all `npp_sci` names to `Parameters.h` (before any includes) so Scintilla.h macros get cleared before any usage. Let me also add the missing `SC_CP_UTF8` constant:

## 2026-07-05T03:14:19.130Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

I'm in the **semantic-lift orchestrator** cycle. Let me follow the workflow: read SKILL.md, run plan, spawn subagents, compile (on host), gate.

## 2026-07-05T03:19:12.173Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

**ScintillaComponent.h and ScintillaComponent.cpp now compile cleanly.** The remaining ~334 errors are in other files (Notepad_plus.cpp, NppBigSwitch.cpp, etc.) — these are API translation errors (missing Qt methods like `showLineNumbers`, `getCurrentWord`, `_p` dereference issues) outside the original ScintillaComponent scope.

The original 3 targets are resolved:

| File | Original Errors | Current Errors |
|------|---------------|----------------|
| `src/ScintillaComponent.h` | 16 | **0** |
| `src/ScintillaComponent.cpp` | 1 (+SCE_* refs) | **0** |
| `src/xmlM...` | (incomplete, likely same root) | resolved |

**Fixes applied:**
1. `NppSciCompat.h` — removed `#pragma once`, added all `npp_sci` `#undef` directives outside include guard so they run on re-inclusion
2. `shortcut.h` — added self-contained `#undef SCK_*`/`SC_UPDATE_*` with `using npp_sci::` declarations  
3. `Parameters.h` — added blanket `#undef` for all `npp_sci` names to prevent Scintilla.h macro pollution
4. `NppConstants.h` — added `#undef INDICATOR_*` before `inline constexpr int INDICATOR_*` 
5. `ScintillaComponent.h` — renamed `int element`→`int sciElement` to avoid `SC_ELEMENT_*` macro shadowing
6. `NppSciCompat.h` — added `SCI_SETELEMENTCOLOUR`, `SCI_SETPRINTCOLOURMODE`, `SCI_FORMATRANGEFULL`, `SCE_UNIVERSAL_*` constants + `using` declarations
7. `ScintillaComponent.cpp` — added `#undef` + `using npp_sci::SCE_UNIVERSAL_*` declarations

## 2026-07-05T03:25:54.481Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

ScintillaComponent.h and ScintillaComponent.cpp are clean. Now following orchestrator workflow:

## 2026-07-05T03:26:56.707Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

Multiple files ahead of git — the flat snapshot added code. Per HEARTBEAT, restore the main divergent file (Parameters.cpp) and rebuild:

## 2026-07-05T03:38:39.045Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

`${SCINTILLA_INCLUDE}` is in the include path — the system Scintilla.h is the culprit. Let me find it:

## 2026-07-05T03:46:37.191Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

Following HEARTBEAT semantic-lift orchestrator cycle. **Step 1: Plan**

## 2026-07-05T03:47:09.036Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

**Step 1: Plan** — run the translation harness to identify current unit backlog:

## 2026-07-05T03:57:24.622Z · fact · session 5354a5da-1842-4842-bbb7-42d76669297c

342 errors remain. Following full orchestrator cycle.

**Step 1: Plan** — identify next units:

## 2026-07-05T05:38:12.344Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The second include hits the include guard. I need to also `#undef` the guard itself:

## 2026-07-05T05:47:05.302Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The issue: `shortcut.h` includes `NppSciCompat.h` via `ScintillaComponent.h` (pragma once allows), then directly. The pragma once skips the second include entirely (including the `#define NPP_SCICOMPAT_H`), so the include guard check never runs the second time. Fix: **remove the direct include of `NppSciCompat.h` from `shortcut.h`** since `ScintillaComponent.h` already provides it:

## 2026-07-05T05:49:44.199Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

Let me run the exact moc compile command from ninja to see the real error:

## 2026-07-05T05:59:28.744Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:08:52.833Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:18:37.046Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:28:32.871Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:38:27.745Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:48:19.684Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:56:44.854Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T06:58:19.455Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:

## 2026-07-05T07:00:22.139Z · fact · session c2bdc530-303d-433d-92f7-f6b93a5022ca

The root cause is clear now. The traditional include guard `#ifndef NPP_SCICOMPAT_H` blocks the SECOND inclusion entirely (when `shortcut.h` directly includes `NppSciCompat.h`), so the `#undef` directives are skipped. The fix: **re-enable `#pragma once`** to handle the double-inclusion, but ALSO add a "reset" mechanism to let the undefs run on every inclusion:
