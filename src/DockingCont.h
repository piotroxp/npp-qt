// npp-qt: forwarding header — DockingCont lives in WinControls/DockingWnd/
#pragma once
#include "WinControls/DockingWnd.h"

// Canonical name in DockingWnd.h is DockingContainer.
// Provide the shorter alias used throughout the codebase.
using DockingCont = DockingContainer;
